#include "miniwarpx.h"

/*
  This function is called before each time step. Users can modify it
  to do anything which needs to be done before the step is taken.

 */
void
before_step(Run_Data& rd, FArray<double>& q, double t)
{
}
